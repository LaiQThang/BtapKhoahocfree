#include <iostream>
using namespace std;

int main ()
{
	int n;
	cout << " nhap so tu nhien n :";
	cin >> n;
		if ( n%2 == 0)
			cout << " so chan la :" << n << endl;
		else 
			cout <<  " so le la:" << n; 
	return 0;
}
